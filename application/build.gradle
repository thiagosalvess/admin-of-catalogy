plugins {
    id 'java-conventions'
    id 'java-library'
}

group = 'com.thiagosalvess.admin.catalogy.application'

dependencies {
    implementation(project(":domain"))

    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}
